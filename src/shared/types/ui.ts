export type ThemeType = 'default' | 'primary' | 'success' | 'warning' | 'error';
export type ImageCarouselState = 'auto' | 'paused' | 'zoomed';

export interface TabItem {
    id: string;
    title: string;
}
